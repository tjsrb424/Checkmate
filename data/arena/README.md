# Arena Reports

Arena league outputs are generated here by:

```bash
npm run arena:league
```

Quick smoke run:

```bash
npm run arena:league:quick
```

Generated `arena-league.json`, `arena-league.csv`, and `arena-league.md` files are not committed. Use the Markdown report to compare standings, warnings, and beta readiness.
